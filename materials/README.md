# Materials

This folder stores static reference files that support the project but are not part of the executable analysis pipeline.

Current contents:

- `Standard.png`: static image asset
- `consent_form.pdf`: participant consent form
- `fNIRS_project.pdf`: project/study reference document

These files are not generated by the scripts in `scripts/`.
They should stay in `materials/` rather than at the repository root.

